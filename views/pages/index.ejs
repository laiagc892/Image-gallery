<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://kit.fontawesome.com/c889951493.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    
    <title>Gallery XL</title>

    <style>
        .nav-link:hover {
            color: coral !important;
        }

    </style>
</head>

<body class="container">
    <header>
        <%- include('../partials/header'); %>
    </header>
    <div class="d-flex flex-row">
        <% photos.forEach( (photo)=> { %>
                <div class="mx-3">
                    <p class="my-2"><%= photo.name.toUpperCase() %> - <%= photo.date %></p>
                    <img src='<%= photo.url %>' alt="<%= photo.name %>">
                    <p style="background-color: <%= photo.color %>"><%= photo.color %></p>
                    <div class="d-flex justify-content-evenly ">
                        <a href="/photos/delete-photos/<%= photo.id %>"><i class="fas fa-trash-alt" ></i></a>
                        <a href="/photos/edit-photos/<%= photo.id %>"><i class="fas fa-edit"></i></a>
                    </div>
                </div>
        <% }); %>
    </div>
            
                          
</body>

</html>